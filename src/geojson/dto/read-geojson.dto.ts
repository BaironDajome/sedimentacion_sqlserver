import { IsNotEmpty, IsString, IsArray, ValidateNested } from 'class-validator';
// Importa decoradores de 'class-validator' para validar las propiedades del DTO.

import { Type } from 'class-transformer';
// Importa el decorador 'Type' de 'class-transformer' para manejar la transformación de objetos anidados.
